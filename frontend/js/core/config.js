// ── CONFIG ──
// Extraído de frontend/index.html (líneas ~456, sección "/* ── CONFIG ── */"
// del <script> monolítico). Contenido idéntico al original: el código viejo
// en index.html NO fue tocado ni borrado (ver notas de la Fase 2 del plan
// de migración) -- este módulo es una copia autocontenida, lista para que
// las fases 3-6 la importen cuando quede conectada vía <script type="module">.

export const API_BASE = '/api';
