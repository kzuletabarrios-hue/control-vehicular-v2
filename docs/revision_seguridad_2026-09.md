# Revisión completa del repo — seguimiento

Iniciada 2026-09-14, a pedido de Karen, tras el hallazgo del permiso no
autorizado en producción (ver `supabase/migrations/20260914140000_...sql`)
y el drift de esquema encontrado en la auditoría de Jorge/María del mismo
día. Este archivo es la fuente de verdad del progreso — si se retoma esta
tarea en otra sesión o después de que se pierda contexto de conversación,
leer esto primero antes de asumir nada.

Plan definido por Alejandro (arquitecto), ver su análisis completo en el
historial de la conversación del 2026-09-14 si hace falta el detalle
completo del razonamiento. Resumen del plan:

- **Fase 1 — Seguridad backend transversal (María)**, prioridad más alta.
  - Lote 1A: `auth.py`, `uploads.py`, `rondas.py`, `proveedores.py`
  - Lote 1B: `control_acceso.py`, `export.py`, `bd_maestros.py`, `dashboard.py`, `carga_masiva.py`
  - Lote 1C: `busqueda.py`, `novedades.py`, `visitantes.py`, `visita_vehicular.py`, `conductores.py`, `flota.py`, `muelles.py`, `citas.py`, `proveedores_publico.py`
- **Fase 2 — Salud de esquema/queries (Jorge)** — independiente, puede ir en paralelo a la Fase 1.
- **Fase 3 — Integridad del frontend modularizado (María)** — después de Fase 1.
- **Fase 4 — UX/accesibilidad (Laura)** — al final.

## Hallazgos ya confirmados en el reconocimiento inicial de Alejandro (previos a Fase 1, no repetir)

1. `busqueda.py`, `novedades.py`, `uploads.py` no usan `require_permiso()` — solo exigen login, no rol.
2. `rondas.py` usa un sistema paralelo propio (`_require_roles()`), no el estándar — pendiente confirmar si es equivalente.
3. `proveedores_publico.py` sin `get_current_user` en ningún endpoint — parece intencional (autorregistro público), pendiente confirmar que no se coló nada sensible.
4. `uploads.py` no valida tamaño ni contenido real del archivo (solo Content-Type declarado).
5. CORS abierto (`allow_origins=["*"]`) en `main.py`.
6. `/docs` y `/redoc` expuestos sin protección.
7. Sin rate limiting en ningún endpoint.
8. Cobertura de tests del backend: prácticamente cero (solo el guardrail anti-producción).

## Estado por lote

| Fase | Lote/bloque | Estado | Agente | Notas |
|---|---|---|---|---|
| 1 | 1A (auth, uploads, rondas, proveedores) | ✅ completo | María | 2026-09-14. Incluye barrido extra de `proveedores_publico.py`. Hallazgo crítico (login sin bloqueo) corregido, desplegado y verificado en producción el mismo día. Ver hallazgos abajo. |
| 1 | 1B (control_acceso, export, bd_maestros, dashboard, carga_masiva) | ✅ completo | María | 2026-09-14. Sin hallazgos críticos/altos — ver detalle abajo. |
| 1 | 1C (resto de routers) | ✅ completo | María | 2026-09-14. Sin hallazgos críticos/altos — ver detalle abajo. |
| **1** | **FASE 1 COMPLETA (1A+1B+1C)** | **✅ cerrada — 2026-09-14** | **María** | **Único hallazgo crítico de toda la fase (fuerza bruta en login) corregido de punta a punta: esquema aplicado en producción por Karen, código en `ed1dbb2` desplegado a `origin/master`, login normal verificado sin regresión por Karen tras el deploy. Resto de hallazgos (altos/medios/bajos) quedan documentados como diagnóstico para priorizar en un batch de hardening posterior — no se cerraron en esta fase, solo el crítico.** |
| 2 | Salud de esquema/queries | ✅ completo | Jorge | 2026-09-14. Diagnóstico únicamente (igual que Fase 1), sin fixes aplicados. Ver hallazgos abajo. |
| 3 | 3A (`core/api-client.js`, `core/auth-store.js`, `shell/LoginPage.js`) | ✅ completo | María | 2026-09-14. Diagnóstico únicamente, sin fixes aplicados. Ver hallazgos abajo. Queda pendiente el resto de `frontend/js/` (`pages/`, `shared/`) para un lote 3B. |
| 3 | 3B (resto del frontend: `pages/`, `shared/`) | 🔲 pendiente | María | — |
| 4 | UX/accesibilidad | 🔲 pendiente | Laura | — |

## Hallazgos confirmados (se agregan aquí conforme avanza cada lote)

### Lote 1A — María, 2026-09-14 (`auth.py`, `uploads.py`, `rondas.py`, `proveedores.py` + barrido extra de `proveedores_publico.py`)

Diagnóstico únicamente, sin fixes aplicados todavía. Prioridad crítico > alto > medio > bajo.

**CRÍTICO**

1. ✅ **CERRADO — 2026-09-14 (esquema aplicado + código desplegado + verificado en producción).** `backend/routers/auth.py`, endpoint `POST /login` (líneas 137-204): sin rate limiting ni lockout por intentos fallidos. No hay ningún control (ni en el endpoint ni a nivel de `main.py`, confirmado — no existe `slowapi` ni middleware de límite en el repo) que impida fuerza bruta contra contraseñas, y las contraseñas mínimas son de solo 8 caracteres sin más requisitos (líneas 293-294, 338, 382-383). Dado el historial reciente de accesos no autorizados en este proyecto, es el hallazgo de mayor prioridad de todo el lote.

   **Fix aplicado (código listo, pendiente de que Karen apruebe aplicar la migración a producción):**
   - Esquema propuesto por Jorge: `supabase/migrations/20260914150000_usuarios_bloqueo_login_columns.sql` (`usuarios.intentos_fallidos INTEGER`, `usuarios.bloqueado_hasta TIMESTAMPTZ`). Aplicada primero contra el Postgres local de pruebas (`bash scripts/dev_test_db.sh reset`) para implementar y probar; **aplicada y verificada en producción por Karen vía SQL Editor** el mismo día (28 usuarios, 0 bloqueados tras aplicar — sin efecto sobre cuentas existentes, tal como se documentó en el propio archivo de migración).
   - Lógica implementada en `backend/routers/auth.py` (`POST /login`, líneas ~150-260): si la cuenta ya está bloqueada (`bloqueado_hasta` futuro) se rechaza con `423` + header `Retry-After`, **sin llamar a `verify_password()`**; password incorrecta con email existente incrementa `intentos_fallidos` en una sola sentencia `UPDATE ... CASE`; al llegar a `LOGIN_INTENTOS_MAX = 5` se fija `bloqueado_hasta = NOW() + 15 min` (`LOGIN_BLOQUEO_MINUTOS`, constantes de aplicación al inicio del archivo, ajustables sin tocar esquema); email inexistente sigue devolviendo el mismo `401` genérico sin tocar ninguna fila; login exitoso resetea `intentos_fallidos`/`bloqueado_hasta` fusionado con el `UPDATE` de `ultimo_acceso` que ya existía. El bloqueo expira solo (no requiere job de limpieza).
   - Umbral/duración: se mantuvieron los valores sugeridos por Jorge (5 intentos / 15 min) — margen razonable para un guarda que se equivoca tecleando, corto para un script de fuerza bruta; no se encontró motivo para ajustarlos.
   - Test funcional nuevo: `backend/tests/test_auth_login_bloqueo.py` (primer test funcional real del proyecto además del guardrail anti-producción) — corrido contra el entorno local, **nunca contra producción**. Cubre: login exitoso resetea contador, login fallido incrementa, 5º fallido consecutivo bloquea, login durante bloqueo se rechaza con 423 sin invocar `verify_password()` (verificado con mock), el bloqueo expira solo pasados los 15 min, y email inexistente sigue en 401 genérico sin tocar la BD. `8 passed` (6 nuevos + 2 del guardrail existente).
   - Fuera de alcance a propósito (decisión de Jorge/Alejandro, queda para el batch de hardening al cierre de la Fase 1): histórico forense de cada intento en `audit_log`, y rate-limiting por IP para emails inexistentes (este mecanismo protege la cuenta real, no mitiga "email spraying" contra cuentas que no existen).
   - **Despliegue y verificación (2026-09-14):** commit `ed1dbb2` (`backend/routers/auth.py`, `backend/tests/test_auth_login_bloqueo.py`, `backend/requirements-dev.txt`, la migración de Jorge) creado localmente por María; el `push` a `origin/master` (que dispara el auto-deploy) lo ejecutó el coordinador con autorización directa de Karen confirmada en el hilo de trabajo — no lo ejecutó María, siguiendo el límite de esta revisión de no tocar producción sin autorización verificable de Karen. Confirmado por `git fetch origin master`: `origin/master` tiene `ed1dbb2` como HEAD. Tras el deploy, Karen probó el login normal en producción real y confirmó que funciona igual que siempre (sin regresión). El bloqueo por 5 intentos fallidos (15 min) quedó validado end-to-end contra el entorno local antes del deploy (`backend/tests/test_auth_login_bloqueo.py`, 8/8 tests en verde) — no se ejecutó una prueba de fuerza bruta real contra producción como parte de esta revisión.

**ALTO**

2. `backend/routers/uploads.py`, `POST /upload-foto` (líneas 69-88): no valida tamaño del archivo (ni antes de decodificar el base64 ni antes de subir a Storage — ningún límite de bytes en el código, y tampoco hay límite de tamaño de body a nivel de FastAPI/Starlette en `main.py`), y no valida que el contenido decodificado sea realmente una imagen (no hay `Pillow`/`python-magic`/`imghdr` en el proyecto — confirmado por búsqueda en todo `backend/`). El `Content-Type: image/jpeg` que se manda a Supabase Storage (línea 59) es un valor fijo puesto por el propio backend, no una validación real de los bytes: cualquier archivo binario arbitrario (ejecutable, script, etc.) puede subirse disfrazado de `.jpg` y quedar servido públicamente desde el bucket. Tampoco hay límite de cantidad de subidas por usuario/tiempo. El endpoint solo exige `get_current_user` (login), sin `require_permiso` — cualquier rol autenticado puede usarlo sin restricción.
3. `backend/routers/auth.py`, `POST /refresh` (líneas 207-242): el `refresh_token` no rota en cada uso. Se emite uno solo en el login (línea 165) y sigue siendo válido hasta su expiración (30 días, `REFRESH_EXPIRE`) o hasta un logout/cambio de contraseña explícito. No hay rotación con detección de reuso (patrón estándar: cada refresh emite un token nuevo e invalida el anterior). Si un refresh token se filtra, queda utilizable en silencio durante semanas.
4. `backend/routers/rondas.py`, `GET /hoy` (líneas 806-830): es el único endpoint del archivo que NO usa `_require_roles(...)` — usa solo `get_current_user` (autenticación, sin control de rol), rompiendo el patrón que sí siguen los otros 20 endpoints del mismo archivo. Hoy el impacto práctico es bajo porque el filtro `WHERE r.recorredor_id = :uid` limita lo que ve un rol fuera de `ROLES_GESTION`, pero es una inconsistencia real frente al resto del router y debería usar `_require_roles(*ROLES_RONDA)` como todos los demás.

**MEDIO**

5. `backend/routers/rondas.py`, `_require_roles()` (líneas 65-70) vs `require_permiso()` de `auth.py` (líneas 121-132): no es un bypass hoy, pero es un mecanismo estructuralmente distinto y más frágil — compara el string `user['rol']` contra una tupla hardcodeada en código (`ROLES_GESTION`, `ROLES_RONDA`), en vez de leer la acción concreta del JSON `permisos` de la tabla `roles` en BD. Dos consecuencias concretas: (a) no distingue lectura de escritura — cualquier rol en `ROLES_GESTION` obtiene automáticamente TODAS las acciones de gestión del módulo (crear/editar/eliminar puntos, cancelar rondas de otros, ver panel/reporte), sin granularidad; (b) bypasa por completo el "clamp" de solo-lectura para `coordinador` que sí se aplica al resto del backend vía `ROLES_SOLO_LECTURA_EN_ESTA_APP` en `auth.py` (líneas 66-89), porque ese clamp solo reduce el dict `permisos`, nunca toca `user['rol']`. No es explotable hoy porque `'coordinador'` no está en `ROLES_GESTION`/`ROLES_RONDA`, pero es una trampa de diseño: si en el futuro se agrega `coordinador` a `ROLES_GESTION` solo para darle visibilidad de `/panel` o `/reporte-semanal`, heredaría automáticamente permisos de escritura (crear/editar/eliminar puntos, cancelar rondas ajenas) sin que nadie lo note ni pueda evitarlo ajustando el JSON de permisos en BD, a diferencia de cómo funciona en el resto del sistema.
6. `backend/routers/auth.py`, `POST /login` (línea 158): posible enumeración de usuarios por timing. `if not user or not verify_password(...)` hace cortocircuito en Python: si el email no existe, `verify_password` (bcrypt, costoso) nunca se ejecuta; si el email existe pero la contraseña es incorrecta, sí se ejecuta el hash completo. La diferencia de tiempo de respuesta es medible y permite inferir qué emails están registrados.
7. `backend/routers/proveedores_publico.py`, `GET /conductor-frecuente` (líneas 56-81): permite enumerar PII de conductores (nombre, teléfono, tipo de vehículo, empresa) por número de cédula. Está protegido con `validar_token_sesion_registro` (token de sesión de 30 min), pero no hay límite de intentos dentro de esa ventana — quien obtenga un token válido (cualquiera que escanee el QR físico de portería) puede probar cédulas en bucle durante media hora.
8. `backend/routers/proveedores_publico.py`, `POST /autorregistro` (líneas 170-373): el token de sesión de registro (`SESION_REGISTRO_TIPO`, JWT stateless, líneas 134-150) no es de un solo uso — nada marca un token como "consumido" tras un autorregistro exitoso. Dentro de su ventana de 30 minutos, el mismo token puede reutilizarse para enviar múltiples autorregistros, sin límite de tasa, permitiendo inyectar registros falsos en `proveedores`.
9. `backend/routers/proveedores.py`, `GET /` (`listar`, líneas 252-300): el parámetro `limit` no tiene tope superior — un cliente autenticado con permiso de lectura puede pedir `limit=999999999` y forzar una consulta muy pesada. No es un problema de control de acceso, pero sí de resiliencia/DoS de bajo costo de explotación.
10. Campos de texto libre sin límite de longitud aplicado en el backend antes de insertar: `observaciones`, `nombre_conductor`, `empresa`, `actividad_a_desarrollar`, `dependencia_autoriza` en `proveedores.py`/`proveedores_publico.py`, y `motivo` en `EditarCitaBody` (proveedores.py líneas 703-730) y en `rondas.py` (`ciclo_pausar`/`ciclo_cancelar`, líneas 548-620). Solo dependen del tipo de columna en Postgres (a confirmar con Jorge) para poner un límite real. No se confirmó en este lote si el frontend renderiza alguno de estos campos con `dangerouslySetInnerHTML` (fuera del alcance de los 4 archivos revisados) — si en algún punto sí ocurre, esto se convierte en vector de XSS almacenado; queda como pendiente de verificación cruzada en la Fase 3 (frontend).

**BAJO**

11. `backend/routers/auth.py`: contraseña mínima de 8 caracteres sin ningún otro requisito de complejidad. Aceptable como criterio aislado (NIST prioriza longitud sobre complejidad), pero combinado con el hallazgo crítico #1 (sin rate limiting/lockout) reduce el margen de seguridad real del login.
12. `backend/routers/proveedores.py`, `confirmar_autorregistro` (línea ~1380): `muelle_descargue` no valida rango 1-18 al confirmarse desde el tablero — es un tema de integridad de datos, no de control de acceso, se documenta por completitud.

**Aspectos revisados y descartados como problema (no requieren acción)**

- SQL injection: todas las consultas dinámicas de los 4 archivos (incluida `editar_usuario` en auth.py y `actualizar`/`listar` en proveedores.py, que arman `SET`/`WHERE` con f-strings) construyen la lista de columnas a partir de listas blancas fijas en código (`CAMPOS_VEHICULO`, `CAMPOS_ORDEN`, chequeos explícitos `if "campo" in body`), nunca de claves arbitrarias del body, y los valores siempre van parametrizados (`:param`). No se encontró ningún punto de inyección.
- Escalación de privilegios vía `proveedores.py`: el allowlist de campos de `crear()`/`actualizar()`/`agregar_orden()`/`actualizar_orden()` excluye explícitamente `creado_por`, `estado_confirmacion`, `rol`, `permisos` — no hay forma de que un guarda modifique esos campos vía body. Todos los endpoints de escritura de `proveedores.py` (incluidos `liberar-muelle`, `asignar-muelle-inversa`, `liberar-muelle-inversa`) están detrás de `require_permiso()` correctamente.
- `proveedores_publico.py` en general: la ausencia de `get_current_user` en todo el archivo es intencional (autorregistro público por QR) y no se encontró ningún endpoint que se haya colado sin querer — la validación server-side de la cita (`_buscar_cita_hoy`, líneas 84-142) impide que el conductor invente `empresa`/`cita_id`. Los hallazgos reales de este archivo (#7 y #8 arriba) son de rate limiting/reuso de token, no de falta de autenticación.
- `bcrypt` con `rounds=12`, normalización `$2a$`→`$2b$` para compatibilidad con hashes generados por `crypt()` de Postgres/Supabase: correcto.
- Manejo de JWT de acceso (`create_access_token`/`decode_token`, auth.py líneas 44-61): expiración, tipo de token (`"type": "access"`) y algoritmo fijo (`HS256`) validados correctamente; `SECRET_KEY` no tiene fallback hardcodeado y el proceso falla al arrancar si falta o es corta (líneas 17-22) — buena práctica.
- CORS abierto y `/docs`/`/redoc` expuestos: ya reportados por Alejandro en el reconocimiento inicial, no se repiten aquí; siguen vigentes.

### Lote 1B — María, 2026-09-14 (`control_acceso.py`, `export.py`, `bd_maestros.py`, `dashboard.py`, `carga_masiva.py`)

Sin hallazgos críticos ni altos en este lote. Los 5 archivos usan `require_permiso()` de forma consistente en absolutamente todos los endpoints de escritura/lectura/exportación, y todas las sentencias `UPDATE`/`INSERT` dinámicas (`control_acceso.py`, `bd_maestros.py`, `carga_masiva.py`) arman el `SET`/columnas a partir de listas blancas fijas en código, nunca de claves arbitrarias del body — no se encontró SQL injection, endpoint sin proteger, ni ruta de escalación de privilegios.

**MEDIO**

1. `backend/routers/export.py`: riesgo de inyección de fórmulas Excel/CSV ("Formula Injection" / DDE) en los ~14 endpoints de exportación (patrón repetido `ws.append(list(row))`, ej. líneas 126, 187-191, 230, 268, 421, 459-461, 497-499, 537, 576, 619, 673-679). Los valores de columnas de texto libre (`observaciones`, `descripcion`, `responsable`, `nombre`, `contratista`, `actividad_a_desarrollar`, `dependencia_autoriza`, etc. — las mismas ya señaladas sin límite de longitud en el Lote 1A) se escriben tal cual en las celdas del `.xlsx` generado por `openpyxl`, sin neutralizar un posible prefijo `=`, `+`, `-` o `@`. Si alguno de esos campos fue cargado con una fórmula maliciosa por un usuario con permiso de escritura en cualquiera de los módulos exportables, y luego un admin/supervisor abre el Excel exportado (permiso `"export"`) en un cliente de Excel de escritorio, esa fórmula puede ejecutarse (clásico vector de "CSV/Excel injection" contra quien abre el archivo, no contra el backend en sí).
2. `backend/routers/carga_masiva.py`: los 4 endpoints de importación (`/conductores`, `/vehiculos`, `/empleados`, `/tiendas`) devuelven el texto crudo de la excepción capturada al cliente — `str(e)[:120]` dentro de la lista `errores` (líneas 85, 150, 223, 278) y `str(e)` completo en el `HTTPException(500, ...)` si falla el `commit()` final (líneas 91, 156, 229, 284) — en vez de traducirlo a un mensaje de negocio como sí hace `proveedores.py` (`_raise_error_insercion`). Puede filtrar detalles internos de la base de datos (nombres de columnas/constraints, tipos) a cualquier usuario con permiso de escritura sobre el módulo importado.

**BAJO**

3. `backend/routers/carga_masiva.py` (líneas 84, 149, 222, 277): dentro del `for` de cada importación, el `except` de una fila individual llama `db.rollback()` — esto revierte TODA la transacción de la sesión (todas las filas previas del mismo lote ya ejecutadas-pero-no-commiteadas en ese `for`), no solo el `INSERT`/`UPDATE` de la fila que falló, porque el único `commit()` real ocurre una sola vez al final del loop. Efecto práctico: si la fila N del archivo falla, las filas 1..N-1 que el contador de respuesta (`insertados`/`actualizados`) ya cuenta como exitosas pueden desaparecer silenciosamente de la base de datos. No es un hallazgo de control de acceso (por eso va en "bajo" dentro de esta revisión de seguridad), pero es un bug de integridad de datos real que vale la pena que el equipo priorice aparte — lo dejo señalado aquí porque lo encontré en este barrido, no porque sea in-scope de la Fase 1 de seguridad.
4. `backend/routers/carga_masiva.py`: el array `filas` de cada importación no tiene límite superior de tamaño por request — mismo patrón ya señalado en el Lote 1A para `limit` en `GET /proveedores` (vector de DoS de bajo costo, no de control de acceso).

**Aspectos revisados y descartados como problema**

- `dashboard.py`: ambos endpoints (`/resumen`, `/tiempo-autorregistro`) detrás de `require_permiso("dashboard","read")`, todas las consultas parametrizadas. Sin hallazgos.
- `bd_maestros.py`: los 12 endpoints (distribución/tiendas, bd_proveedores, bd_control_acceso, conductores_frecuentes) usan `require_permiso("maestros", ...)` consistentemente con la acción correcta (`read`/`write`/`delete`); los `DELETE` son en realidad soft-delete (`activo=FALSE`/`estado='INACTIVO'`), consistente con el resto del sistema. Sin hallazgos.
- `control_acceso.py`: incluida la función Postgres `fn_control_acceso_anular` invocada desde `PUT /{id}/anular` — el endpoint solo traduce el `RAISE EXCEPTION` a HTTP, la regla de negocio (permiso, motivo obligatorio, ventana de tiempo, no doble anulación) vive en la función de BD; queda fuera del alcance de este lote revisar el cuerpo de esa función (es objeto de Jorge/Fase 2), pero el wrapper en Python no introduce ningún bypass. Sin hallazgos de acceso.

### Lote 1C — María, 2026-09-14 (`busqueda.py`, `novedades.py`, `visitantes.py`, `visita_vehicular.py`, `conductores.py`, `flota.py`, `muelles.py`, `citas.py`)

Sin hallazgos críticos ni altos. `visitantes.py`, `visita_vehicular.py`, `conductores.py`, `flota.py`, `muelles.py` y `citas.py` usan `require_permiso()` de forma consistente y correcta en todos los endpoints (incluida la granularidad `read`/`write`/`delete`), con columnas de `UPDATE` siempre desde listas blancas fijas — sin SQL injection, sin endpoint sin proteger, sin escalación de privilegios. `flota.py` además registra auditoría (`_audit`) en sus 4 endpoints de escritura, buen extra que no tienen otros routers equivalentes.

**Corrección a un hallazgo superficial de Alejandro (no un hallazgo nuevo):**

1. `backend/routers/busqueda.py`, `GET /` (líneas 12-146): el reconocimiento inicial lo listó junto a `uploads.py`/`novedades.py` como "solo exige login, no rol". Revisado a fondo: **no es así del todo** — el endpoint sí filtra por permiso de lectura por módulo (`if "read" in permisos.get("flota", [])`, `permisos.get("proveedores", [])`, `permisos.get("control_acceso", [])`, `permisos.get("visita_vehicular", [])`, `permisos.get("visitantes", [])`, líneas 26, 44, 63, 110, 127), leyendo el mismo dict `permisos` del usuario que usaría `require_permiso()`. El efecto de seguridad es equivalente: un usuario sin `"read"` en un módulo simplemente no recibe resultados de ese módulo. La diferencia real es de **estilo/duplicación**, no de control de acceso roto: esta lógica está reimplementada inline en vez de usar la dependencia compartida `require_permiso()`, así que si en el futuro `require_permiso()`/`get_current_user()` cambia su criterio (ej. un nuevo clamp de solo-lectura como el de `coordinador`), `busqueda.py` no lo hereda automáticamente y hay que acordarse de replicarlo aquí también. Recomendación de bajo esfuerzo para un futuro refactor: extraer esa comprobación a un helper compartido en `auth.py` en vez de dejarla flotando en este archivo — no urgente, sin riesgo real hoy.

**BAJO / informativo**

2. `backend/routers/novedades.py`: `GET /` y `POST /` (líneas 21-90) usan solo `get_current_user` (sin `require_permiso`) — es intencional, no un descuido: no existe un módulo `"novedades"` en el esquema de `permisos` de `roles` (ver `supabase/migrations/20260601101430_auth_schema.sql`), y el diseño es que cualquier usuario autenticado pueda reportar una novedad (autorreporte de incidentes) y ver las suyas propias; solo `ROLES_GESTION` (`admin`/`supervisor`) ve todas. `PATCH /{id}/estado` (líneas 93-116) sí restringe correctamente a `ROLES_GESTION`, aunque con el mismo patrón de chequeo de rol hardcodeado en vez de `require_permiso()` que ya se señaló como riesgo de diseño en `rondas.py` (Lote 1A, hallazgo medio #5) — acá el radio de impacto es mínimo (un solo endpoint, una sola acción de cambio de estado), así que no se eleva de severidad, solo se deja anotado por consistencia con el resto de la revisión.

**Aspectos revisados y descartados como problema**

- `visitantes.py`, `visita_vehicular.py`, `conductores.py`: CRUD estándar, `require_permiso()` en cada endpoint con la acción correcta, sin hallazgos.
- `flota.py`: incluye `POST /{id}/duplicar` (copia un registro completo a uno nuevo) — revisado, requiere `flota:write`, no permite tocar `creado_por` del original más allá de sobreescribirlo con el usuario actual, sin ruta de escalación.
- `muelles.py`: router 100% de lectura (`require_permiso("muelles","read")` en sus 2 endpoints), sin mutación de estado — no hay superficie de escritura que auditar en este archivo (documentado en el propio código: la escritura real vive en `proveedores.py`, ya revisado en el Lote 1A).
- `citas.py`: `PUT /config` (único endpoint de escritura del archivo) documenta en su propio docstring que verificó contra la tabla `roles` real que hoy solo `admin` puede usarlo con éxito en `control-vehicular-v2` (el clamp de solo-lectura de `coordinador` en `auth.py` lo bloquea pese a que la fila compartida de `roles` diga "write") — coherente con el resto del sistema, sin hallazgos.

### Fase 2 — Jorge, 2026-09-14 (salud de esquema/queries: `rondas.py`, `proveedores.py`, `export.py`, `dashboard.py`, `busqueda.py`)

Diagnóstico únicamente, igual que la Fase 1 de María — ningún fix aplicado ni ninguna migración nueva creada. No repito el hallazgo ya cerrado de "22 llaves foráneas sin cobertura" (`20260911130929_indices_llaves_foraneas.sql`) ni el de "`limit` sin tope en `GET /proveedores`" (Lote 1A, hallazgo #9), ambos ya documentados. Tampoco repito el hallazgo de `creado_por` sin FK vs. `creado_por_fk` huérfana — ya está investigado y documentado a fondo por una sesión previa de Jorge en `20260911130900_creado_por_fk_columnas_huerfanas.sql` y `20260807220625_proveedores_estado_ingresado_wps.sql`.

Contexto de volumen (de la propia auditoría de hoy): `control_acceso` ~7.7k filas es hoy la tabla más grande (nota de `20260911130929`), `proveedores` ~1400 filas al 2026-08-01. Son tablas chicas todavía, pero `rondas`/`rondas_ciclos` crecen rápido por diseño (9 puntos × hasta 7 rondas/turno × 2 turnos/día × N recorredores → cientos de filas/día solo en `rondas`), así que priorizo pensando en la trayectoria de crecimiento, no solo en el dolor de hoy.

**ALTO**

1. **`backend/routers/busqueda.py` (líneas 27-33, 45-52, 64-69, 67, 80-85, 95-100, 111-116, 128-133): búsqueda global con `ILIKE '%q%'` (comodín al INICIO) sobre 8 tablas (`flota_propia`, `proveedores`+`proveedores_ordenes`, `control_acceso`, `sustancias`, `herramientas`, `visita_vehicular`, `visitantes`), incluido un `CAST(cedula AS TEXT) ILIKE` (línea 67, 131).** Un `ILIKE` con `%` al inicio nunca puede usar un índice B-tree normal, y confirmé que la extensión `pg_trgm` **no está instalada en ningún script** (`grep` de `CREATE EXTENSION` en toda `supabase/migrations/` solo encuentra `uuid-ossp`, `20260601101420_schema_base.sql` línea 8). Este endpoint dispara hasta 8-9 `SELECT` distintos por cada letra tecleada (patrón típico de "buscar mientras escribes", `q` mínimo de 2 caracteres), cada uno haciendo *seq scan* completo de su tabla. Hoy las tablas son chicas y no se nota; en 6-12 meses de operación del CEDI con `control_acceso`/`proveedores`/`rondas` creciendo, cada tecla escrita en el buscador va a escanear varias tablas completas en serie. Recomendación: `CREATE EXTENSION IF NOT EXISTS pg_trgm;` + índices `GIN` trigram sobre las columnas de texto usadas (`placa`, `conductor`, `nombre_conductor`, `po.empresa`, `nombre`, `contratista`, `descripcion`, `responsable`, etc.) y, para `cedula` (que se castea a texto), un índice funcional `GIN` sobre `CAST(cedula AS TEXT)` o cambiar la búsqueda de cédula a comparación exacta/prefijo en vez de `%...%`.

2. **`backend/routers/proveedores.py`, `GET /` (`listar`, líneas 252-300): la rama "abiertos" del `UNION ALL` (`SELECT * FROM base WHERE hora_salida IS NULL`, línea 280) no tiene índice de cobertura ni límite propio.** `proveedores` (`supabase/migrations/20260601101420_schema_base.sql`, columna `hora_salida` línea 121) solo tiene índices en `fecha DESC` y `empresa` (líneas 132-133) — nada en `hora_salida`. Como este endpoint es el listado principal de Proveedores (uno de los routers que el propio pedido marca como "más pesado") y por defecto se llama sin filtro de `fecha`, la sub-consulta `WHERE hora_salida IS NULL` sobre `base` (que en el caso sin filtro es la tabla completa) hace *seq scan* completo cada vez, y además **ignora `limit`/`offset` por completo** para esa mitad del resultado (el `LIMIT`/`OFFSET` de los parámetros solo aplica a la rama "cerrados", líneas 282-286) — así que aunque se corrija el hallazgo ya documentado de "`limit` sin tope", la cantidad de vehículos *actualmente dentro* del CEDI siempre se trae completa. Hoy es aceptable porque el número de vehículos simultáneamente dentro es naturalmente chico, pero es una dependencia implícita del modelo de operación, no algo garantizado por el esquema. Recomendación: índice parcial `CREATE INDEX ON proveedores (fecha DESC, created_at DESC) WHERE hora_salida IS NULL;` (cubre el filtro y el `ORDER BY` a la vez) y, si el volumen de "abiertos" simultáneos llega a crecer, aplicar el mismo `LIMIT`/`OFFSET` a esa rama.

3. **`backend/routers/rondas.py`: combinación de patrón N+1 real + falta de índice compuesto en el camino más transitado de escritura del router (arrancar/pausar/reanudar/cancelar/marcar una ronda).**
   - **N+1 en `panel()` (líneas 935-1029)**: por cada recorredor activo (`for u in recorredores:`, línea 949) se ejecutan 3 queries (`ciclos`, `apoyos`, `ultima_marcacion`, líneas 950-952, 973-976, 979-984) más **una llamada adicional a `_analizar_ciclo()` (línea 959) por CADA ronda completada de ese recorredor**, que a su vez hace su propio `SELECT ... FROM rondas WHERE ciclo_id = :cid` (línea 101-103). Con 10 recorredores activos y 5 rondas completadas c/u en el turno, esto son 10×3 + 50 = 80 queries en un solo `GET /panel` — endpoint de supervisión que típicamente se refresca solo (panel en vivo). Se puede resolver con 1-2 queries agregadas (agrupando por `recorredor_id` con `GROUP BY`/window functions) en vez del loop.
   - **N+1 en `reporte_semanal()` (líneas 1032-1114)**: mismo patrón — por cada recorredor (línea 1048) se ejecutan 3 queries, incluida la CTE pesada de `sospechosas` (líneas 1062-1090, con `LAG() OVER` sobre 7 días de `rondas`). Se puede reescribir como una sola consulta agregada por `recorredor_id` con `GROUP BY`.
   - **Índice compuesto/parcial faltante para `rondas_ciclos WHERE recorredor_id = :uid AND estado IN ('en_curso','pausada') ORDER BY hora_inicio DESC LIMIT 1`**: esta consulta exacta (o su variante de un solo estado) se repite en `ciclo_activo` (470-474), `ciclo_iniciar` (512-514), `ciclo_pausar` (557-560), `ciclo_reanudar` (577-580) y `ciclo_cancelar` (607-610) — es decir, corre en *casi cada acción* que hace un recorredor con la app. Hoy solo existen índices de una sola columna sobre `recorredor_id`, `fecha` y `estado` (`20260623155722_recorredor_turnos.sql` líneas 40-42), así que Postgres tiene que combinarlos con bitmap AND en vez de resolver el filtro con un solo índice. Recomiendo un índice parcial: `CREATE INDEX ON rondas_ciclos (recorredor_id, hora_inicio DESC) WHERE estado IN ('en_curso','pausada');` — los ciclos activos/pausados son siempre una fracción mínima de la tabla (la mayoría termina en `completa`), así que el índice parcial es liviano y cubre exactamente este patrón.
   - En el mismo espíritu, `turno_cronograma` (412-416) y el conteo de `completadas` en `ciclo_iniciar` (530-533) filtran por `(recorredor_id, fecha, turno)` sin índice compuesto, y `apoyo_marcar` (881-885) / `ciclo_iniciar` (518-521) filtran `apoyos_operativos` por `(recorredor_id, hora_salida IS NULL)` sin índice compuesto — mismo patrón, menor frecuencia relativa.

**MEDIO**

4. **`backend/routers/dashboard.py`, `GET /resumen` (líneas 106-213): 4 de las 5 consultas de "detalle" traen la tabla completa (sin `LIMIT` en SQL) y recién truncan a 30/20 filas en Python.** `acceso_hoy_rows` (121-126) se corta con `[:30]` en la línea 134, `prov_hoy_rows` (136-145) con `[:30]` en 156, `visitantes_hoy_rows` (158-163) con `[:30]` en 171 — en los tres casos Postgres devuelve TODAS las filas de hoy (sin tope) y Python descarta el resto. Pero **`acceso_dias_anteriores_rows` (líneas 175-180) no tiene ningún límite, ni en SQL ni en Python** — es la lista de accesos sin registrar salida de días anteriores, que por diseño se va acumulando si nadie las cierra (es justamente el propósito del bloque: mostrar lo que "se perdía del límite de 100 del listado principal", según el propio comentario de la línea 174). Si ese backlog crece (turnos con mucha rotación, guardas que olvidan marcar salida), este endpoint —que es el dashboard principal, se carga en cada visita— devuelve una lista sin tope. Recomendación: mover el `LIMIT` a SQL en los 3 primeros casos (evita traer y descartar filas de más) y agregar un `LIMIT` explícito (ej. 50-100) a la consulta de `acceso_dias_anteriores`.

5. **`backend/routers/export.py`: los 16 endpoints de exportación (`/flota`, `/proveedores`, `/control-acceso`, `/visitantes`, `/visita-vehicular`, `/sustancias`, `/herramientas`, `/novedades`, `/apoyos`, `/rondas`, `/citas`, más los 5 sin filtro de fecha en absoluto: `/conductores`, `/bd-tiendas`, `/bd-vehiculos`, `/bd-empleados`, `/bd-proveedores`) no tienen ni un rango de fechas por defecto ni un tope de filas.** `fecha_desde`/`fecha_hasta` son opcionales en todos (ej. línea 58-59, 135-136, etc.) y, si no se envían, el `WHERE` queda en `"1=1"` — exporta el histórico completo. Cada endpoint hace `.fetchall()` (carga todo el resultset en memoria de Python de una vez) y arma el `.xlsx` completo con `openpyxl` en memoria antes de responder (`_stream`, líneas 45-53). No es un bug de acceso (los 5 sin filtro de fecha son catálogos/maestros, naturalmente chicos), pero `/rondas`, `/control-acceso` y `/proveedores` sí acumulan histórico operativo día a día y hoy no tienen ningún tope — es cuestión de tiempo antes de que un "exportar todo" sin filtro se vuelva una consulta lenta y un archivo pesado de generar y descargar. Recomendación: exigir `fecha_desde`/`fecha_hasta` (o aplicar un rango por defecto razonable, ej. últimos 90 días) en los endpoints que exportan tablas de crecimiento diario.

6. **`backend/routers/rondas.py` / esquema: 4 columnas de texto libre que el código trata como enum cerrado, sin `CHECK` en la base de datos.** `rondas.estado` (`ok`/`novedad`/`omitido`, validado solo en Python en `marcar_punto`, línea 667-668 de `rondas.py`; columna definida en `20260616233250_rondas_novedades.sql` línea 46, `TEXT NOT NULL DEFAULT 'ok'`, sin `CHECK`), `rondas_ciclos.estado` (`en_curso`/`pausada`/`completa`/`cancelada`/`incompleta`, columna en `20260623155722_recorredor_turnos.sql` línea 36, sin `CHECK`; `20260624125705_pausa_ronda.sql` línea 10 solo lo *comenta* en prosa, nunca agrega el `CHECK`), `rondas_ciclos.turno` (`dia`/`noche`, línea 32 del mismo archivo, sin `CHECK`) y `apoyos_operativos.tipo` (`automatico`/`manual`, línea 56, sin `CHECK`). Cualquier `UPDATE`/`INSERT` directo contra la base (SQL Editor de Supabase, script manual, error de programación futuro) puede dejar un valor fuera de catálogo sin que la base de datos lo impida — a diferencia de `proveedores.estado_confirmacion` o `citas_programadas.estado`, que sí tienen su `CHECK` versionado. Dado que este mismo proyecto ya tuvo casos de escritura manual directa a producción (ver hallazgos de drift del propio LÉAME), no es un riesgo puramente teórico.

7. **`rondas`/`rondas_ciclos`/`apoyos_operativos`/`novedades`: `ON DELETE CASCADE` desde `usuarios(id)`, inconsistente con el patrón `SET NULL` que usa el resto del esquema para columnas de auditoría.** `rondas.recorredor_id`/`rondas.punto_id` (`20260616233250_rondas_novedades.sql` líneas 41-42), `novedades.usuario_id` (línea 59 mismo archivo), `rondas_ciclos.recorredor_id` (`20260623155722_recorredor_turnos.sql` línea 30) y `apoyos_operativos.recorredor_id` (línea 51 mismo archivo) están todas en `ON DELETE CASCADE`: si algún día se borra físicamente una fila de `usuarios` (hoy no hay ningún endpoint de `DELETE` sobre `usuarios` en `auth.py` — confirmado por búsqueda —, pero sí hay antecedente reciente de ediciones manuales directas vía SQL Editor en este mismo proyecto), se borra en cascada TODO el historial de rondas/apoyos/novedades de ese usuario — exactamente el tipo de rastro que un sistema de control de acceso no debería poder perder. Contrasta con `creado_por`, `anulado_por`, `marcado_wps_por`, `liberado_por`, `asignado_por`, etc. en el resto del esquema, que consistentemente usan `ON DELETE SET NULL` para preservar el registro histórico aunque el usuario deje de existir. Recomendación: cambiar estas 4 FK a `ON DELETE RESTRICT` (obliga a desactivar/archivar el usuario en vez de borrarlo mientras tenga historial) o, si se prefiere permitir el borrado físico, a `SET NULL` igual que el resto del esquema — nunca `CASCADE` sobre datos de auditoría. Es un cambio de `ALTER TABLE ... DROP CONSTRAINT` + `ADD CONSTRAINT` que no requiere tocar datos existentes.

8. **`backend/routers/rondas.py`, `GET /historial` (líneas 833-854): `LIMIT 500` fijo sin `offset` (no hay forma de pedir la página siguiente) y, cuando no se pasa `fecha` (parámetro opcional), el `ORDER BY r.fecha DESC, r.created_at DESC` no tiene índice de cobertura completo** — `rondas` solo tiene índice de una columna en `fecha DESC` (`idx_rondas_fecha`, `20260616233250_rondas_novedades.sql` línea 53), no en `(fecha DESC, created_at DESC)`, así que Postgres usa el índice para el primer criterio de orden y hace un *sort* adicional en memoria para el segundo. Con volumen bajo hoy no se nota; a medida que `rondas` crece, filtrar sin `fecha` (que es exactamente el caso de uso "ver todo el historial reciente") se vuelve cada vez más lento, y además cualquier fila más allá de las primeras 500 (ordenadas por fecha) queda inaccesible para siempre por la falta de paginación real.

9. **`backend/routers/proveedores.py`, `GET /citas/alertas` (líneas 593-700): dos huecos de rendimiento en la misma consulta.** (a) El filtro principal `WHERE cp.fecha = :hoy AND ... AND cp.estado = 'pendiente'` (líneas 652-655) solo tiene cobertura de índice en `fecha` sola (`idx_citas_programadas_fecha`, `20260804100000_schema_tablas_huerfanas_drift.sql` línea 222) — no existe un índice compuesto `(fecha, estado)` ni parcial `WHERE estado = 'pendiente'`, a pesar de que este endpoint se diseñó explícitamente para refrescarse seguido (es la fuente de las alertas de "por vencer"/"atrasada" del guarda). (b) El subselect correlacionado a `bd_proveedores` (líneas 640-648) compara `UPPER(btrim(bp.nombre)) = UPPER(btrim(cp.proveedor_nombre))` — una expresión sobre ambos lados que ningún índice normal sobre `bd_proveedores.nombre` puede usar; se ejecuta una vez POR CADA fila de `citas_programadas` que matchea el filtro principal. Con el catálogo de proveedores actual (chico) no importa, pero es una operación intrínsecamente no indexable con el diseño actual. Recomendación: `CREATE INDEX ... ON citas_programadas (fecha) WHERE estado = 'pendiente';` para (a); para (b), si `bd_proveedores` crece, considerar un índice funcional `ON bd_proveedores (UPPER(btrim(nombre)))` o, mejor, normalizar `nombre` a mayúsculas/sin espacios en una columna generada.

**BAJO**

10. `backend/routers/rondas.py`: `rondas.lat`/`rondas.lng` (`DOUBLE PRECISION`, `20260707162828_rondas_gps.sql` líneas 7-8) no tienen `CHECK` de rango válido (`lat` entre -90/90, `lng` entre -180/180). Estos valores alimentan directamente `_haversine_m()` y el cálculo de "ronda sospechosa" (líneas 82-140) — una coordenada corrupta (bug de GPS del navegador, valor manipulado en el body del `PUT /marcar`, que no valida rango en ningún punto de `marcar_punto`, líneas 646-734) podría distorsionar silenciosamente esas métricas de auditoría en vez de ser rechazada. Impacto bajo porque hoy nada consume esas métricas para bloquear una acción, solo para reportar.
11. `backend/routers/proveedores.py`, `POST /batch` (`crear_batch`, líneas 1622-1666): inserta fila por fila dentro de un `for` (línea 1634, un `db.execute()` de `INSERT` por registro) en vez de un único `INSERT` multi-fila — funcionalmente correcto (misma transacción, mismo commit), solo más lento que necesario para lotes grandes. Endpoint marcado como "legacy... kept for backward compat" (línea 1621), impacto bajo.
12. `muelle_eventos.muelle_id`/`muelle_eventos.proveedor_id` (`20260804100000_schema_tablas_huerfanas_drift.sql` líneas 237-238) son `NOT NULL REFERENCES ...` sin cláusula `ON DELETE` explícita (default `NO ACTION`, equivalente a `RESTRICT`). Hoy es inofensivo porque la tabla tiene 0 filas en producción (confirmado en el propio comentario del archivo, línea 229) y no se escribe activamente, pero si algún día se retoma su uso, un `DELETE FROM proveedores` (`backend/routers/proveedores.py` líneas 1158-1168) fallaría en vez de completarse para cualquier proveedor con un evento de muelle asociado, sin que el código del endpoint lo anticipe o traduzca a un mensaje de negocio.

**Aspectos revisados y descartados como problema (no requieren acción)**

- `backend/routers/export.py`: no se encontró ningún patrón de N+1 — las 16 exportaciones son cada una un único `SELECT` (con `JOIN`/`LEFT JOIN` donde corresponde), el loop sobre `rows` solo escribe en el `Workbook`, no dispara consultas adicionales por fila.
- `backend/routers/proveedores.py`, `_attach_ordenes`/`_attach_muelle` (líneas 186-249): a pesar de acompañar a un listado, NO son N+1 — ambas hacen un único `SELECT ... WHERE ... IN (...)` por lote de IDs en vez de una consulta por fila. Buen patrón, ya usado consistentemente en `listar()` y `obtener()`.
- `backend/routers/proveedores.py`, `POST /citas/importar` (líneas 796-991): el `SAVEPOINT` por fila (`db.begin_nested()`, línea 951) es un patrón correcto para lotes con validación en dos niveles (Python + BD) sin perder las filas ya insertadas — no es el mismo bug de "rollback total" que María reportó en `carga_masiva.py` (Lote 1B, hallazgo medio/bajo #3).
- `backend/routers/rondas.py`, función `fn_control_acceso_anular` y el esquema de anulación de `control_acceso` (`20260722172923_anulacion_control_acceso.sql`): fuera del alcance de los 5 routers pedidos, pero lo revisé de pasada por la mención pendiente de María en el Lote 1B — está bien diseñado (índices parciales ya cubren exactamente el patrón de `listar()`, `CHECK` de integridad de la anulación, trigger que bloquea revertir, función atómica con auditoría incluida). Sin hallazgos.

### Lote 3A — María, 2026-09-14 (`frontend/js/core/api-client.js`, `frontend/js/core/auth-store.js`, `frontend/js/shell/LoginPage.js`)

Diagnóstico únicamente, sin fixes aplicados todavía — igual que las fases anteriores. Prioridad crítico > alto > medio > bajo. Los 3 archivos son código legado extraído literal del `<script>` monolítico de `index.html` durante la modularización (fase 6, ya mergeada) — ningún hallazgo de este lote fue introducido por la modularización en sí, ya existían en el monolito.

**Punto 1 del pedido — manejo del nuevo 423 del login: revisado, NO es el bug genérico que se sospechaba.** `LoginPage.js` (línea 36: `if(!r.ok){ const e=await r.json(); throw new Error(e.detail||'Error al ingresar'); }`) no distingue por `r.status` en el código, pero tampoco hace falta: usa el campo `detail` que ya manda el backend, y `backend/routers/auth.py` (líneas 196-203) ya redacta un mensaje específico para el 423 ("Cuenta bloqueada temporalmente por múltiples intentos fallidos. Intenta de nuevo en N minuto(s).") distinto del 401 genérico ("Credenciales incorrectas", línea 221). El usuario bloqueado SÍ ve el mensaje correcto, no uno confuso de "credenciales inválidas". Quedan dos gaps reales, documentados como medio/bajo más abajo (#6 y #8): no se lee el header `Retry-After`, y no hay ninguna diferencia visual (icono, cuenta regresiva, deshabilitar el botón) entre un bloqueo temporal y una contraseña incorrecta — solo cambia el texto.

**ALTO**

1. ✅ **CORREGIDO — 2026-09-14 (autorizado por Karen).** `frontend/js/shell/LoginPage.js`, línea 23: `const [email,setEmail] = useState('admin@cedirex.com');` — el campo de email del login venía precargado por defecto con lo que aparenta ser el email real de la cuenta admin de producción. Cualquiera que abría la pantalla de login (o inspeccionaba el código servido) veía ese valor. Esto filtraba un nombre de usuario válido y de máximo privilegio, exactamente el dato que el hallazgo medio #6 del Lote 1A (enumeración de usuarios por timing en `POST /login`) señala como protegido hoy solo por el tiempo de respuesta — con el email ya expuesto en el propio formulario, ese vector de timing se volvía irrelevante para esa cuenta: un atacante no necesitaba enumerarla, ya la tenía. Combinado con el hallazgo crítico ya cerrado de fuerza bruta (Lote 1A #1), el admin era el blanco más obvio y más fácil de apuntar de todo el sistema.

   **Fix aplicado:** `useState('')` en vez de `useState('admin@cedirex.com')` — el campo ahora inicia vacío, como cualquier input de login normal (el `placeholder` ya era genérico: `'usuario@empresa.com'`, línea 57, no se tocó). Cambio de una sola línea, sin build step (ES Modules nativos servidos directo, confirmado que `frontend/index.html` ya no tiene el `<script>` monolítico embebido — solo 23 líneas, carga `js/main.js` como `<script type="module">`, así que no hay una segunda copia del default en el propio `index.html` que también corregir). Verificado que nada más depende de ese valor precargado: sin tests de frontend en el repo (no hay `package.json` de frontend ni archivos `*.test.js`), y la validación de `handleLogin` (línea 29, `if(!email||!pass)`) funciona igual con string vacío. Las únicas otras apariciones de `admin@cedirex.com` en el repo son el seed real de la cuenta admin en `supabase/migrations/20260601101430_auth_schema.sql`/`database/auth_schema.sql` y la mención en `README.md` (documentación de onboarding) — ninguna depende del valor del input, no se tocaron. Cambio puramente estático de frontend, sin tocar backend ni producción (no requiere deploy de servidor, solo que se sirvan los archivos estáticos actualizados).
2. `frontend/js/core/api-client.js`, líneas 33-47: si la llamada de red al refresh (`fetch(`${API_BASE}/auth/refresh`...)`, línea 36) falla por un problema transitorio de conectividad (el `catch(e){}` de la línea 44 lo traga en silencio, sin distinguir "token inválido" de "sin red"), el código cae igual a `clearAuth(); window.location.reload();` (líneas 46-47) — cierra la sesión y recarga la página completa como si el token hubiera expirado de verdad. En una PWA usada por guardas en casetas con WiFi/datos inestables, un corte de red momentáneo a mitad de una acción (ej. registrando la salida de un vehículo) fuerza un logout completo, sin ningún aviso ("se perdió la conexión" vs. "tu sesión expiró"), y el `reload()` descarta cualquier dato no guardado que el usuario tuviera en pantalla. Es el hallazgo más importante de este lote junto con el anterior.

**MEDIO**

3. `frontend/js/core/api-client.js`, línea 50 (`if(!r.ok) throw new Error(await r.text());`): para cualquier respuesta no-2xx que no sea 401 (403, 404, 409, 422, y un futuro 423 si algún día se agrega a otro endpoint además de `/login`), el cuerpo crudo de la respuesta (normalmente el JSON `{"detail":"..."}` que sí manda el backend) se convierte tal cual en `Error.message`, sin `JSON.parse` ni extracción de `detail`. Cualquier página que capture ese error y muestre `e.message` al usuario le mostraría el JSON crudo con llaves y comillas en vez de un mensaje legible — inconsistente con `LoginPage.js`, que sí extrae `detail` correctamente. No se revisaron en este lote las páginas de `pages/` para confirmar cuántas exponen este texto crudo al usuario final (queda para el lote 3B).
4. `frontend/js/core/api-client.js`: ningún `fetch` de este archivo (líneas 31, 36, 40, 64) usa `AbortController`/timeout — si el backend no responde, la promesa queda pendiente indefinidamente y la UI (botón en estado "cargando", spinners) puede quedarse así sin límite. Además, la llamada inicial de la línea 31 no está envuelta en `try/catch`: si `fetch()` mismo rechaza (sin red, fallo de DNS — `TypeError: Failed to fetch`), esa excepción no la maneja `_fetch` en absoluto, se propaga cruda a quien llamó `api.get/post/put/del/patch`, y cada página consumidora queda responsable de capturarla por su cuenta sin ningún contrato consistente.
5. `frontend/js/core/api-client.js`, líneas 32-47: si dos o más llamadas a `api.*` están en vuelo al mismo tiempo cuando el token expira (ej. una pantalla que dispara varios `GET` en paralelo al cargar), cada una detecta su propio 401 y dispara su propio `POST /auth/refresh` en paralelo — no hay mutex ni una promesa compartida que deduplique el refresh. Hoy es inofensivo porque el refresh token no rota (ya documentado en Lote 1A, hallazgo alto #3), pero es un patrón fragil: el día que se implemente rotación de refresh token (la recomendación de ese mismo hallazgo), esta falta de deduplicación causaría que la segunda llamada de refresh en paralelo reciba un refresh token ya invalidado por la primera, y ese usuario se desloguearía sin haber hecho nada mal.
6. `frontend/js/core/auth-store.js`, líneas 9-14: `access_token`, `refresh_token` y el objeto `usuario` completo (incluido su `permisos`) se guardan en `localStorage` en texto plano — no en cookie `httpOnly` ni en memoria. No es explotable hoy: se revisaron a fondo los 3 archivos de este lote más `core/icons.js` (el único otro punto de `dangerouslySetInnerHTML` en `core/`, líneas 15-26) y no se encontró ningún renderizado de contenido proveniente de la API sin pasar por el escape automático de React — `icons.js` inyecta únicamente strings SVG estáticos hardcodeados en el propio código fuente (`Icons`, líneas 30-66), nunca datos de usuario/API. **Esto cierra, para el alcance de estos 3 archivos + `icons.js`, el pendiente de XSS almacenado dejado en el Lote 1A (hallazgo medio #10)** sobre `observaciones`/`nombre_conductor`/etc. — en `core/` y `shell/LoginPage.js` no hay ningún vector. Pero el pendiente sigue abierto para donde esos campos realmente se muestran: `pages/ProveedoresPage` (o su módulo `pages/proveedores/index.js`), `pages/VisitaVehicularPage.js`, `pages/RondaPage.js`/`RondaPanelPage.js`, etc. — todo fuera del alcance de este lote 3A, queda como parte explícita del lote 3B.
7. `frontend/js/shell/LoginPage.js`, línea 36: `const e = await r.json();` no tiene su propio `try/catch` — si el backend o algo delante de él (proxy, balanceador) responde con un cuerpo no-JSON en un error (ej. una página HTML de error 502/504), el parseo lanza una excepción de JavaScript que termina cayendo al `catch` genérico de la línea 41 y mostrándole al usuario un mensaje técnico ("Unexpected token '<'...") en vez de algo como "No se pudo conectar con el servidor, intenta de nuevo".

**BAJO**

8. `frontend/js/shell/LoginPage.js`: el 423 se comunica con el texto correcto (ver nota arriba del punto 1 del pedido) pero el header `Retry-After` que sí manda el backend (`backend/routers/auth.py` línea 202) nunca se lee — no hay cuenta regresiva ni el botón "Ingresar" se deshabilita durante el bloqueo. El usuario puede seguir dando clic sin ningún efecto dañino (el backend, líneas 193-203, rechaza de inmediato sin tocar `intentos_fallidos` ni extender el bloqueo mientras la cuenta ya está bloqueada), pero es una oportunidad de UX perdida y, sin ella, un usuario bloqueado puede pensar que la app está fallando en vez de entender que debe esperar.
9. `frontend/js/core/api-client.js`, línea 39: al refrescar el token dentro de `_fetch` solo se actualiza `cv_token` en `localStorage`; el campo `expires_in` que el backend sí retorna en `/auth/login` (`backend/routers/auth.py` línea 262) nunca se guarda ni se usa en el frontend — el modelo de expiración es 100% reactivo (esperar a que llegue un 401), sin refresco proactivo antes de que el token expire.
10. `frontend/js/core/api-client.js`, línea 74: `exportar()` usa `alert('Error al exportar: '+e.message)` — el `alert()` nativo del navegador es una UX abrupta y bloqueante, inconsistente con el patrón de error inline (`div.login-error`) que sí usa `LoginPage.js`. Bajo impacto porque es un flujo secundario (exportación), se documenta por completitud.

**Aspectos revisados y descartados como problema**

- Refresco automático de sesión: confirmado que si el access token expira a mitad de una acción, `api-client.js` (líneas 32-49) intenta renovarlo solo, sin que el usuario tenga que hacer nada — reintenta la petición original una sola vez con el token nuevo. Diseño correcto en el camino feliz; los gaps reales están en el manejo de fallas de ese mismo mecanismo (hallazgos alto #2 y medio #5 arriba).
- `frontend/js/core/config.js`: `API_BASE = '/api'` — ruta relativa, sin URL/host/credenciales hardcodeadas. Sin hallazgos.
- No se encontró ningún `console.log`/`console.warn`/`console.error` en ninguno de los 3 archivos (ni en `icons.js`) que exponga el token, el refresh token o el objeto de usuario — búsqueda dirigida en todo `frontend/js/` sin resultados.
- `frontend/js/App.js` (revisado de pasada por ser el consumidor directo de `auth-store.js`/`api-client.js`, aunque no es uno de los 3 archivos del lote): la cola offline (`getQueue`/`addToQueue`/`clearQueue`) sí está conectada de punta a punta — `addOffline` la alimenta desde páginas como `FlotaPage.js` (línea 130) y `syncQueue` (líneas 99-112) la vacía llamando a `api.post`/`api.put` — no es una funcionalidad huérfana como parecía a primera vista solo mirando `auth-store.js`. Es un flujo manual (botón "Sincronizar ahora", línea 172), no automático al recuperar conexión; queda fuera de alcance evaluar si eso es correcto por ser parte de `App.js`/`pages/`, no de los 3 archivos pedidos.



(vacío todavía)
